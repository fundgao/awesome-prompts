# Netlify 部署配置 - Next.js 16 + pnpm
# 文档: https://docs.netlify.com/build/frameworks/framework-setup-guides/nextjs/overview/

[build]
  command = "pnpm run build"

[build.environment]
  NODE_VERSION = "20"
  # pnpm 与 Next.js 兼容：允许依赖提升，避免构建时找不到模块
  PNPM_FLAGS = "--shamefully-hoist"
